#include<iostream>
using namespace std;
int main()
{
    int i;
    string str="aabhgudrr";
    int arr[50];
    int len=str.size();
    for(i=0;i<len;i++)
    {
        if(arr[str[i]==str[i+1]])
        
            return 1;
        else 
        return 0;
    }
    if(arr[i]==0)
    cout<<arr[i];
  return 0;  
}